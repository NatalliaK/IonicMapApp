import React from 'react';
import { IonCard, IonCardContent } from '@ionic/react';

const NotFound: React.FC = () => (
  <IonCard>
    <IonCardContent>Page not found!</IonCardContent>
  </IonCard>
);

export default NotFound;
